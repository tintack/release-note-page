# release notes for gRPC-go

```
git clone https://github.com/menghanl/release-note-page
cd release-note-page
# npm install
# npm run build

cd src/backend
GITHUB_TOKEN=<github_token> go run main.go
```
